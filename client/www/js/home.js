var LetsDoThis = LetsDoThis || {};

$(document).on("pagecreate", "#page-home", function(event) {
    
    // var userData = LetsDoThis.Session.getInstance().getLoggedInUser();
    // var eventData = LetsDoThis.Session.getInstance().getEvent();
    
    // $("#page-home").append("<p>Let's Do This, "+userData.token+"!</p>");
    // $("#page-home").append("<p>Have fun at  "+eventData.name+"!</p>");
});  